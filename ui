import streamlit as st
st.title("Superstore Sales Query Tool")
# Input box
user_input = st.text_input("Ask a question about Superstore sales data:")
# Submit button
if st.button("Submit"):
    st.write("You asked:", user_input)
    # Here you'll call your backend FastAPI or SQL handler
